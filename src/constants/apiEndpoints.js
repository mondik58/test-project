const REQUEST_URL = 'https://jsonplaceholder.typicode.com';
const ENDPOINTS = {
  USERS: `${REQUEST_URL}/users`,
  POSTS: `${REQUEST_URL}/posts`,
  GALLERY: `${REQUEST_URL}/albums`,
};

export default ENDPOINTS;
